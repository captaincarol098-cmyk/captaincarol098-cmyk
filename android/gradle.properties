org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Gradle performance tuning (added by assistant)
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
# Keep the jvmargs but make them a bit more conservative on Windows if needed
org.gradle.jvmargs=-Xmx6G -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
# Kotlin daemon options
kotlin.daemon.jvm.options=-Xmx2G
